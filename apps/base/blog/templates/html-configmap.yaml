apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-html
  namespace: {{ include "common.names.namespace" . }}
data:
  index.html: |
{{ .Files.Get "files/index.html" | indent 4 }}

