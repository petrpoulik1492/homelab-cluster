apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blog-nginx
  namespace: blog
spec:
  interval: 1m
  chart:
    spec:
      chart: ./apps/base/blog
      sourceRef:
        kind: GitRepository
        name: apps
        namespace: flux-system
  values:
    persistence:
      enabled: true
      size: 1Gi
