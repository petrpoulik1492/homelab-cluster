apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared
  namespace: audiobookshelf 
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloudflared
  template:
    metadata:
      labels:
        app: cloudflared
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --config
            - /etc/cloudflared/config/config.yaml
            - run
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            failureThreshold: 1
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/cloudflared/config
              readOnly: true
            - name: creds
              mountPath: /etc/cloudflared/creds
              readOnly: true
      volumes:
        - name: creds
          secret:
            secretName: audiobookshelf-container-env 
        - name: config
          configMap:
            name: audiobookshelf-configmap
            items:
              - key: config.yaml
                path: config.yaml
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared
  namespace: audiobookshelf 
data:
  config.yaml: |
    tunnel: audiobookshelf 
    credentials-file: /etc/cloudflared/creds/credentials.json
    metrics: 0.0.0.0:2000
    no-autoupdate: true
    ingress:
    - hostname: audiobookshelf.petrpoulik1492.com
      service: http://audiobookshelf:3005 
    - hostname: hello.example.com
      service: hello_world
    - service: http_status:404
