apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nginx
  namespace: helm
spec:
  interval: 5m
  chart:
    spec:
      chart: nginx
      version: "15.3.1"
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: helm
  values:
    replicaCount: 1

    image:
      registry: docker.io
      repository: bitnami/nginx
      tag: "1.25.2"
      pullPolicy: IfNotPresent

    service:
      type: NodePort
